function countNumber(string) {
	let array=string.split('');
	let a=[];
	for (let i=0;i<array.length;i++) {
         if(array[i] === isNaN) {
          a.push(array[i]) 
      } 
    }
    let start=a[0];
    let count=0;
    for (let i=0;i<a.length;i++) {
     if(a[i]===start) {
 count++; 
}
     start=a[i]
    return ("a[i]"+count)
     }
	}
countNumber('erer384jjjfd123');